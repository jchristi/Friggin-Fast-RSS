/*var php = require('phpjs');

process.on('uncaughtException', function(err) {
  if(error_reporting() === 0) return false;
  var file = __filename;
  if (global.hasOwnProperty('logger')) {
    return logger.log_error(errno, err);
  }
});*/
